# Classess Gateway service — the wall (Ring 0).
fastapi>=0.111,<0.120
uvicorn[standard]>=0.30,<0.36
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
httpx>=0.27,<0.29
PyJWT[crypto]>=2.8,<3
# Audit sink to Postgres (guarded import; degrades to a buffered logger sink).
asyncpg>=0.29,<0.31
