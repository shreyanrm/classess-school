# Python test + runtime toolchain for the spine engines and capability modules.
# Install once into a venv: python -m venv .venv && . .venv/bin/activate && pip install -r requirements-dev.txt
pytest>=8
pytest-asyncio>=0.23
pydantic>=2
pydantic-settings>=2
fastapi>=0.110
httpx>=0.27
