import { VidyaHome } from './_components/VidyaHome';

/**
 * The Classess School home — conversation-first and calm. The home is the
 * conversation, the front door over the stable spine of pages reachable from
 * the rail.
 */
export default function HomePage() {
  return <VidyaHome />;
}
