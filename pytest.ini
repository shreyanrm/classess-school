[pytest]
# Each spine service / module owns its own `app` package and tests, so the suite
# is run per-module (see scripts/test-python.sh). This root config sets shared
# defaults that apply when pytest discovers it as the rootdir.
asyncio_mode = auto
addopts = -q
testpaths = tests
