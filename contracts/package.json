{
  "name": "@classess/contracts",
  "version": "0.1.0",
  "description": "Classess School Ring 0 contracts — the single source of truth. Immutable event/evidence schemas, OpenAPI capability specs, canonical DB schema descriptions, and design tokens. Every other module binds to this package.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./events": "./src/events/index.ts",
    "./openapi": "./src/openapi/index.ts",
    "./tokens": "./src/tokens/index.ts"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
