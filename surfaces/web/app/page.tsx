import { RoleLanding } from './_components/RoleLanding';

/**
 * The Classess School home — the role's calm "today" landing: a greeting,
 * briefing cards, suggestion chips, and quick links. The conversation now lives
 * in the floating Vidya orb (mounted globally in the layout), present on every
 * page; the home is no longer a central chat space.
 */
export default function HomePage() {
  return <RoleLanding />;
}
