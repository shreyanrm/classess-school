/**
 * The academic ontology contract and the Slice 1 seed.
 *
 * Board-agnostic by construction: the board is a labelled node, never a baked-in
 * enum. The seed proves the narrow path (one board, one grade, two subjects).
 */

export * from "./types.js";
export * from "./seed.js";
