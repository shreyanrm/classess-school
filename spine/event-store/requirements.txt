# Classess Event Store service (Ring 0).
fastapi>=0.111,<0.120
uvicorn[standard]>=0.30,<0.36
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
httpx>=0.27,<0.29
PyJWT[crypto]>=2.8,<3
# Append-only write + governed read over Postgres (guarded; degrades to a
# clearly-labelled append-only in-memory log).
asyncpg>=0.29,<0.31
