# Classess Identity service (Ring 0).
# Pinned to compatible minor ranges; the orchestrator installs centrally.
fastapi>=0.111,<0.120
uvicorn[standard]>=0.30,<0.36
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
httpx>=0.27,<0.29
PyJWT[crypto]>=2.8,<3
# Data client for the PII vault + platform-canonical tables (guarded import:
# the service starts without it and degrades to an in-memory adapter).
asyncpg>=0.29,<0.31
