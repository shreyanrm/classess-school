import { ResetPassword } from './ResetPassword';

export const metadata = { title: 'Set a new password — Classess School' };

export default function ResetPasswordPage() {
  return <ResetPassword />;
}
