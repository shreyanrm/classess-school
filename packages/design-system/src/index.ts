/* ============================================================================
   @classess/design-system — public entry.

   The Classess v4.1 design system: "Steel + one hit of pigment." European-
   minimal. No drop shadows. Sharp corners. One sans. One vivid accent per
   surface. The ultramarine signature is reserved for the brand mark and the
   mastery ignite.

   Styles ship separately and must be imported once at the app root:
       import '@classess/design-system/styles.css';
   ============================================================================ */

export * from './types';
export * from './hooks';
export * from './components';
